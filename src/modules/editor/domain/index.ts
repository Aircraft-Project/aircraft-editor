import type { EditorWorkspace } from "./editor";

export type { EditorResource, EditorResourceKind, EditorWorkspace } from "./editor";

export const DEFAULT_EDITOR_WORKSPACE: EditorWorkspace = "components";
